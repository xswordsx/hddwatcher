package lib

import "fmt"

// GetSpace returns disk size information for the given drive (bytes).
func GetSpace(string) (int64, int64, int64, error) {
	return 0, 0, 0, fmt.Errorf("not implemented")
}
